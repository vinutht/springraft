CREATE TABLE IF NOT EXISTS log (index SERIAL PRIMARY KEY, term INTEGER, command VARCHAR(255), uuid VARCHAR(255));
CREATE TABLE IF NOT EXISTS election_term (id SERIAL PRIMARY KEY, current_term INTEGER, voted_for INTEGER);